import Include from './include'
import If from './if'
import Where from './where'
import Item from './item'
import SetTag from './set'


export default [
  Include,
  If,
  Where,
  Item,
  SetTag,
]


export {
  Include,
  If,
  Where,
  Item,
  SetTag,
}
